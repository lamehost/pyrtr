apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pyrtr.fullname" . }}-test-connection"
  labels:
    {{- include "pyrtr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pyrtr.fullname" . }}:{{ .Values.service.http.port }}']
  restartPolicy: Never
